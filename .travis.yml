language: node_js
dist: trusty
cache:
  yarn: true
  directories:
    - node_modules
node_js:
  - "4"
  - "6"
  - "node"
script:
  - yarn lint
  - yarn test
  - 'if [ "$TRAVIS_NODE_VERSION" = "node" ]; then yarn test:umd; fi'
after_success:
  - "nyc report --reporter=text-lcov | coveralls"
